package util;

import javafx.scene.layout.HBox;
import javafx.scene.layout.VBox;

public class Course extends HBox {

    public Course(String timeStart, String timeEnd, String courseName, String courseLocation, String ){
        HBox hbox = new HBox();
        VBox time = new VBox();
        time.getChildren().add()
    }


    /**
     * HBOX
     * Time
     *       VBOX WITH Label + label
     * COURSE
     *       VBOX Label + space + Label + Bar
     */

}
